{{- if .Values.catalogSync.enabled }}
{{ include "sigil.validateStorageBackend" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sigil.fullname" . }}-catalog-sync
  labels:
    {{- include "sigil.labels" . | nindent 4 }}
    app.kubernetes.io/component: catalog-sync
spec:
  replicas: {{ .Values.catalogSync.replicaCount }}
  selector:
    matchLabels:
      {{- include "sigil.catalogSyncSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "sigil.catalogSyncSelectorLabels" . | nindent 8 }}
        {{- with .Values.catalogSync.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.catalogSync.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "sigil.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: catalog-sync
          image: "{{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.catalogSync.service.ports.http }}
              protocol: TCP
          env:
            {{- include "sigil.commonEnv" (dict "root" . "target" .Values.catalogSync.target "extraEnv" .Values.catalogSync.extraEnv "httpPort" .Values.catalogSync.service.ports.http) | nindent 12 }}
          {{- if or .Values.sigil.extraEnvFrom .Values.catalogSync.extraEnvFrom }}
          envFrom:
            {{- with .Values.sigil.extraEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- with .Values.catalogSync.extraEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.catalogSync.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.catalogSync.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.catalogSync.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.catalogSync.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
