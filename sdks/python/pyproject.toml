[project]
name = "sigil-sdk"
version = "0.1.0"
description = "Sigil Python SDK skeleton"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
